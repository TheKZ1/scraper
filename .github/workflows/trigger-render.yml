name: Trigger Render Deploy

on:
  schedule:
    - cron: '56 15 * * *'  # 16:56 Netherlands time (CET)
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
